{
  "name": "energy-bill-predictor",
  "version": "1.0.0",
  "description": "A Flutter web application for predicting electricity bills",
  "scripts": {
    "build": "flutter build web --release",
    "install": "flutter pub get"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
